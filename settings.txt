You will need to fill out this file for hand in. 
The bellow is just an example, feel free to disregard this documents contents.


Your final settings.txt will look something like this when you are finished:

/////////////////////////////////

Wallpaper 1:

let night = false	  ; // if set false makes it day

let starNumber = 200;  //higher number more stars
let cloudNumber = 50; //higher more clouds
let smallClouds = true; 

let graySky = true //if set false sky is light blue

let moon = true;   
let sunny = true;  

let left_Mountain = true;

let gridSize = 200;

function setup_wallpaper(pWallpaper) {
  pWallpaper.output_mode(GLIDE_WALLPAPER);  //DEVELOP_GLYPH
  pWallpaper.resolution(NINE_LANDSCAPE); //NINE_PORTRAIT // NINE_LANDSCAPE
  pWallpaper.show_guide(false); //set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = gridSize; 
  pWallpaper.grid_settings.cell_height = gridSize;
  pWallpaper.grid_settings.row_offset  = gridSize/2;
}

/////////////////////////////////

Wallpaper 2:

let night = false	  ; // if set false makes it day

let starNumber = 200;  //higher number more stars
let cloudNumber = 500; //higher more clouds
let smallClouds = false; 

let graySky = false //if set false sky is light blue

let moon = true;   
let sunny = true;  

let left_Mountain = true;

let gridSize = 200;


function setup_wallpaper(pWallpaper) {
  pWallpaper.output_mode(GRID_WALLPAPER);  //DEVELOP_GLYPH
  pWallpaper.resolution(NINE_LANDSCAPE);
  pWallpaper.show_guide(false); //set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = gridSize; 
  pWallpaper.grid_settings.cell_height = gridSize;
  pWallpaper.grid_settings.row_offset  = gridSize/2;
}
////////////////////////////////

Wallpaper 3:
let night = false	  ; // if set false makes it day

let starNumber = 200;  //higher number more stars
let cloudNumber = 100; //higher more clouds
let smallClouds = true	; 

let graySky = false //if set false sky is light blue

let moon = true;   
let sunny = true;  

let left_Mountain = true;

let gridSize = 100;


function setup_wallpaper(pWallpaper) {
  pWallpaper.output_mode(GRID_WALLPAPER);  //DEVELOP_GLYPH
  pWallpaper.resolution(NINE_LANDSCAPE);
  pWallpaper.show_guide(false); //set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = gridSize; 
  pWallpaper.grid_settings.cell_height = gridSize;
  pWallpaper.grid_settings.row_offset  = gridSize/2;
}
///////////////////////////////////////

Wallpaper 4

let night = false	  ; // if set false makes it day

let starNumber = 200;  //higher number more stars
let cloudNumber = 100; //higher more clouds
let smallClouds = true	; 

let graySky = false //if set false sky is light blue

let moon = true;   
let sunny = true;  

let left_Mountain = true;

let gridSize = 200;


function setup_wallpaper(pWallpaper) {
  pWallpaper.output_mode(GRID_WALLPAPER);  //DEVELOP_GLYPH
  pWallpaper.resolution(NINE_LANDSCAPE);
  pWallpaper.show_guide(false); //set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = gridSize; 
  pWallpaper.grid_settings.cell_height = gridSize;
  pWallpaper.grid_settings.row_offset  = gridSize/2;
}

//////////////////////////////////////

Wallpaper 5

let night = true	  ; // if set false makes it day

let starNumber = 500;  //higher number more stars
let cloudNumber = 50; //higher more clouds
let smallClouds = true; 

let graySky = true //if set false sky is light blue

let moon = true;   
let sunny = false;  

let left_Mountain = true;

let gridSize = 200;

function setup_wallpaper(pWallpaper) {
  pWallpaper.output_mode(GRID_WALLPAPER);  //DEVELOP_GLYPH //GRID_WALLPAPER //GLIDE_WALLPAPER
  pWallpaper.resolution(NINE_LANDSCAPE); //NINE_PORTRAIT //NINE_LANDSCAPE
  pWallpaper.show_guide(false); //set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = gridSize; 
  pWallpaper.grid_settings.cell_height = gridSize;
  pWallpaper.grid_settings.row_offset  = gridSize/2;
}

/////////////////////////////////

Wallpaper 6

let night = true	  ; // if set false makes it day

let starNumber = 500;  //higher number more stars
let cloudNumber = 50; //higher more clouds
let smallClouds = true; 

let graySky = true //if set false sky is light blue

let moon = false;   
let sunny = false;  

let left_Mountain = true;

let gridSize = 200;

function setup_wallpaper(pWallpaper) {
  pWallpaper.output_mode(GLIDE_WALLPAPER);  //DEVELOP_GLYPH //GRID_WALLPAPER //GLIDE_WALLPAPER
  pWallpaper.resolution(NINE_LANDSCAPE); //NINE_PORTRAIT //NINE_LANDSCAPE
  pWallpaper.show_guide(false); //set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = gridSize; 
  pWallpaper.grid_settings.cell_height = gridSize;
  pWallpaper.grid_settings.row_offset  = gridSize;
}

/////////////////////////////////////////

wallpaper 7

let night = true	  ; // if set false makes it day

let starNumber = 100;  //higher number more stars
let cloudNumber = 50; //higher more clouds
let smallClouds = true; 

let graySky = true //if set false sky is light blue

let moon = true;   
let sunny = false;  

let left_Mountain = true;

let gridSize = 200;

function setup_wallpaper(pWallpaper) {
  pWallpaper.output_mode(GLIDE_WALLPAPER);  //DEVELOP_GLYPH //GRID_WALLPAPER //GLIDE_WALLPAPER
  pWallpaper.resolution(NINE_LANDSCAPE); //NINE_PORTRAIT //NINE_LANDSCAPE
  pWallpaper.show_guide(false); //set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = gridSize; 
  pWallpaper.grid_settings.cell_height = gridSize;
  pWallpaper.grid_settings.row_offset  = gridSize;
}

///////////////////////////////////////////

Wallpaper 9

let night = false	  ; // if set false makes it day

let starNumber = 200;  //higher number more stars
let cloudNumber = 50; //higher more clouds
let smallClouds = true; 

let graySky = true //if set false sky is light blue

let moon = true;   
let sunny = true;  

let left_Mountain = true;

let gridSize = 200;

function setup_wallpaper(pWallpaper) {
  pWallpaper.output_mode(GLIDE_WALLPAPER);  //DEVELOP_GLYPH
  pWallpaper.resolution(NINE_LANDSCAPE); //NINE_PORTRAIT // NINE_LANDSCAPE
  pWallpaper.show_guide(false); //set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = gridSize; 
  pWallpaper.grid_settings.cell_height = gridSize;
  pWallpaper.grid_settings.row_offset  = gridSize/2;
}